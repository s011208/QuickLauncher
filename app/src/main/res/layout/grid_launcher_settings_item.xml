<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:paddingLeft="@dimen/notification_launcher_settings_item_padding_h"
    android:paddingRight="@dimen/notification_launcher_settings_item_padding_h"
    android:layout_height="wrap_content">

    <ImageView
        android:scaleType="centerInside"
        android:id="@+id/icon"
        android:layout_centerHorizontal="true"
        android:layout_alignParentTop="true"
        android:layout_width="@dimen/notification_launcher_settings_icon_size"
        android:layout_height="@dimen/notification_launcher_settings_icon_size" />

    <TextView
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:id="@+id/title"
        android:minLines="2"
        android:maxLines="2"
        android:gravity="center|top"
        android:ellipsize="end"
        android:layout_below="@+id/icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>